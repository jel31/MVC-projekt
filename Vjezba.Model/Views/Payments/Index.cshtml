@model IReadOnlyList<Vjezba.Model.Payment>

@{
    ViewData["Title"] = "Uplate";
}

@section Scripts {
    <script src="~/js/ajax-search.js" asp-append-version="true"></script>
    <script>
        (function () {
            var input = document.getElementById('payments-global-search');
            var results = document.getElementById('payments-search-results');
            if (!input || !results || !window.ajaxSearch) return;

            var endpoint = '/api/search/payments';
            window.ajaxSearch.attach({ input: input, endpoint: endpoint, container: results, pageSize: 12, debounceMs: 300, init: false, mainListSelector: '#payments-main-list' });
        })();
    </script>
}

<section class="page-header" aria-labelledby="payments-title">
    <div>
        <span class="eyebrow">Financijski pregled</span>
        <h1 id="payments-title">Uplate</h1>
        <p class="page-subtitle">Pregled iznosa, načina plaćanja i ishoda svake uplate.</p>
    </div>
    <div class="page-actions">
        <a class="btn btn-primary" href="@Url.Action("Create", "Payments")">Dodaj uplatu</a>
        <a class="btn btn-outline" href="@Url.Action("Index", "Bookings")">Pogledaj rezervacije</a>
        <a class="btn btn-outline" href="@Url.Action("Index", "Home")">Natrag na početnu</a>
    </div>
</section>

<section class="search-section" aria-label="Traži uplate">
    <div id="payments-search-container" class="search-container">
        <label for="payments-global-search" class="sr-only">Pretraži uplate</label>
        <input id="payments-global-search" class="form-control" placeholder="Pretraži po vlasniku, šetaču ili načinu plaćanja..." />
    </div>
    <div id="payments-search-results" class="search-results" aria-live="polite"></div>
</section>

<div id="payments-main-list">
@if (!Model.Any())
{
    <section class="empty-state" aria-live="polite">
        <h2>Još nema uplata</h2>
        <p>Kada se rezervacije završe, ovdje će se pojaviti povezane uplate.</p>
        <a class="btn btn-primary" href="@Url.Action("Index", "Home")">Vrati se na početnu</a>
    </section>
}


else
{
    <section class="entity-grid" aria-label="Popis uplata">
        @foreach (var payment in Model)
        {
            <article class="entity-card">
                <div class="entity-card__title">
                    <div>
                        <h2>Uplata #@payment.Id</h2>
                        <p class="entity-card__meta">@payment.Date.ToString("dd.MM.yyyy HH:mm")</p>
                    </div>
                    <span class="badge-soft">€@payment.Amount.ToString("F2")</span>
                </div>

                <div class="entity-tags">
                    <span class="entity-tag">@payment.PaymentMethod</span>
                    <span class="entity-tag">Rezervacija #@payment.Booking.Id</span>
                </div>

                <p class="entity-card__summary">@(payment.IsSuccessful ? "Uplata je uspješna." : "Uplata nije uspješna.")</p>

                <div class="entity-card__actions">
                    <a class="btn btn-primary" href="@Url.Action("Details", "Payments", new { id = payment.Id })">Detalji</a>
                    <a class="btn btn-outline" href="@Url.Action("Edit", "Payments", new { id = payment.Id })">Uredi</a>
                    <a class="btn btn-outline" href="@Url.Action("Delete", "Payments", new { id = payment.Id })">Obriši</a>
                </div>
            </article>
        }
    </section>
}
</div>